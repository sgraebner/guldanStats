# Beitragen

1. Erstelle ein Issue für größere Änderungen.
2. Erzeuge einen Feature-Branch von `main`.
3. Richte `pre-commit` ein:
   ```bash
   pip install -r requirements-dev.txt
   pre-commit install
   ```
4. Eröffne einen Pull Request mit klarer Beschreibung.
